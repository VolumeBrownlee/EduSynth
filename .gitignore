# ───────────── Dependencies ─────────────
node_modules/

# ───────────── Environment & secrets ─────────────
# Never commit real .env files — they hold API keys, DB passwords, JWT secrets.
.env
.env.*
# ...but DO keep the safe templates:
!.env.example

# ───────────── Build output ─────────────
dist/
build/

# ───────────── Logs ─────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# ───────────── Backend uploads (user files) ─────────────
edusynth-lms/backend/uploads/

# ───────────── OS / editor cruft ─────────────
.DS_Store
Thumbs.db
.idea/
*.swp

# ───────────── Local-only project report ─────────────
# The full academic project report and its Word copy are kept locally for safe-keeping
# but should never be pushed to GitHub.
REPORT.md
EDUSYNTH.docx

